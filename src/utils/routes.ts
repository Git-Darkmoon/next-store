export enum ROUTES {
  HOME = "/",
  ABOUT = "/about",
  ADMIN = "/admin",
  CART = "/cart",
  FAVORITES = "/favorites",
  PRODUCTS = "/products",
  ORDERS = "/orders",
  REVIEWS = "/reviews",
}

export enum ADMIN_ROUTES {
  SALES = "/admin/sales",
  PRODUCTS = "/admin/products",
  CREATE_PRODUCT = "/admin/products/create",
}
